name: Init CI Workflow

permissions:
  contents: read  
  
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  init:
    uses: codeparce/ci-workflows/.github/workflows/node-init.yml@main
    with:
      node-version: 20
